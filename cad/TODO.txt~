*Everything:
    * Import all other parts and do prelim stuff.
    *Change all screw mounts and holes based on empirical cutter measurments
    *Change interior lines to mounts in all files
    *Mount Eee PC, Arduino, Battery, On Button

*Make:
    *Rails

*Rails:
    *Make rails from sides
    *Modify back brace interface.
    *Make sure that the rail interfaces with the braces correctly.

*Sides:
    *Make sure the sides interface with braces correctly.

*Base:
    *Copy interface with side
    *Add interface with cross braces
    *Add encoder sensor mounts
    *Switch holes


* Make sure the back IR/ Button both fit
